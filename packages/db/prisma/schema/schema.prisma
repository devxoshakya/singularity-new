generator client {
  provider = "prisma-client-js"
  moduleFormat = "esm"
  runtime = "workerd"
  engineType = "edge"
}

datasource db {
  provider = "mongodb"
  directUrl = env("DIRECT_URL")
  url      = env("DATABASE_URL")
}

