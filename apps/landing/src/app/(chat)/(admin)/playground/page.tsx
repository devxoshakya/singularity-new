import { PlaygroundThread } from "@/components/playground/playground-thread";

export default function AdminPlaygroundPage() {
    return (
        <main className="flex-1 overflow-hidden">
            <PlaygroundThread />
        </main>
    );
}
